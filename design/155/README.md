# design#155 — the artifacts drawing, section IX: the workspace as a reference target, the workspace dashboards landing, the visible-to-everyone grant

Two frames rendered from `specs/app-artifacts.html` at the pull request's head c51dbda5572f38fb60be240c18fbb15a5818dd10 (1440 css px wide, 2x device scale, light palette only — a design-spec change carries no dark-theme proof). The whole of section IX, top to bottom, in two tiles.

| file | requires | shows | verdict |
|---|---|---|---|
| sec-IX-dashboards-tab-light-part1of2.png | PLAN: Per-Scope Surfaces §14 row for the artifacts drawing §IX: §IX.1 admits the whole-workspace scope as a REFERENCE target; its Dashboards tab behaves exactly like the other scopes' — the per-user shell with the non-removable Overview default and the single Add dashboard popup with three sections (Create new; Reference a dashboard from the scopes below; Add from the installed catalog); Personal keeps Create new and the installed catalog only; the reference contract stated (a reference never widens access by itself; the listing is viewer-filtered). | The section lede now reads "The whole-workspace scope is a reference target: its Dashboards tab behaves exactly like the other scopes' … the same per-user dashboard shell with its non-removable Overview default, and the same single Add dashboard popup, whose Reference a dashboard from the scopes below section is how a lower-scope dashboard is brought up to the workspace"; §IX.1 states the popup's three sections in order and, in a new paragraph, "The workspace is a reference target, and its collection is filtered at read time … a reference never widens access by itself … the listing is viewer-filtered"; the existing team-page, personal-page and add-picker examples are unchanged; §IX.2 names the workspace tab's curators (organization admins their own organization's links, platform administrators all). | PASS |
| sec-IX-dashboards-tab-light-part2of2.png | §IX.3 (new): the workspace Dashboards landing — one paragraph on what changed and why, naming invitation-granular sharing as a follow-up; the workspace page example (the five-entry strip with no Settings, the non-removable Overview row, two referenced rows, one carrying the visible-to-everyone mark) and the Add dashboard popup with its three sections; the access rules of §11.4 stated. §IX.4 (new): the visible-to-everyone grant — the mark on a referenced entry; the set/unset control for a platform administrator (on and off readings) and muted with the reason for everyone else; the non-admin viewer's entry with the grant (listed, read-only) and without it (no row). | "IX.3 · The workspace Dashboards landing — the Overview default and the reference collection" with the paragraph "What changed, and why" ending "Sharing a reference with named people or invitations — invitation-granular sharing — is a named follow-up outside this drawing"; the Workspace example: tabs Dashboards · Assistants · Agents · Artifacts · Skills (no Settings), rows Overview ("the default — always here, and never removable", Open only), Pipeline health — Q3 (Remove · Open), Revenue attribution (Visible to everyone · Remove · Open); the Add dashboard popup with CREATE NEW, REFERENCE A DASHBOARD FROM THE SCOPES BELOW (two rows with Reference), ADD FROM THE INSTALLED CATALOG; the "Access rules of the reference collection" callout with the four rules; "IX.4 · Visible to everyone — the read-only grant on a reference" with the platform administrator's control (toggle on with its reading, toggle off) beside "everyone else — the control is muted, with the reason" ("Only a platform administrator can set or unset who a reference is visible to"); the non-admin viewer "with the grant — listed, and read-only" (Open only) and "without the grant — not listed for this viewer" ("The entry is not in this viewer's list … there is no row — not a locked one"); the closing callout "Read-only, revocable, audited". | PASS |

Graded by the coordinating agent (the model named in the merge record) against PLAN: Per-Scope Surfaces §11 and §14 and the pull request's own list of changes; 2 of 2 PASS. One note, not counted: the existing Personal example's caption still says "there is no add-to-scope", while the lede now says Personal keeps Create new and the installed catalog — the same landed shape read two ways (the caption means no reference section); a wording pass could align the two.
