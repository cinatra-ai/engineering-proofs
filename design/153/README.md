# design#153, second round — the review of the rendered assignment page

Three frames rendered from `specs/app-extensions.html` at the pull request's head 10be7caf8c3994772273b7a9d56ada240bc725b1 (1440 css px wide, 2x device scale, light palette only — a design-spec change carries no dark-theme proof). Whole sections, top to bottom; section VII in two tiles. The first round's set (at 2a631bbb) stays in this folder for the record.

| file | requires | shows | verdict |
|---|---|---|---|
| r2-sec-V-extension-settings-light.png | Section V unchanged this round: no Skills group, the pointer sentence to the assignment page. | The settings-page mock with Permissions, Marketplace, Maintenance and the Danger zone; the prose sentence pointing at the assignment page (section VII). | PASS |
| r2-sec-VII-per-agent-assignment-page-light-part1of2.png | (1) No address line inside any example mock — the address stays in the annotation strip only. (2) No "5 per scope here, at most 5 effective per run." sentence inside any example. (3) The Artifacts pane drawn per manifest slot: one group per slot with its kind and bounds, the Skills chooser, chosen rows in the Skills row chrome, the empty state, a slot at its bound, an artifact no longer available; the assistant page Skills-only with no strip. | The annotation strip still names both address templates and the five scope bases; the three example headers carry the scope, the package and the vendor line and NO address; no limits sentence in any example (the chooser hints "2 of 5 skills chosen" and "5 of 5 skills chosen — remove one to choose another" stay). The Artifacts pane: "Context artifacts" with its one-line explanation; the slot "Brand voice — Takes a brand kit · 1 required, at most 2" with the chooser open on "bra" (two two-line options: Brand Kit 2026 · Brand kit · by Acme Corp; Brand Voice Guide · Brand kit · by Growth) and one chosen row (Tone of Voice 2025 with a remove control); the slot "Reference posts — Takes blog posts · optional, at most 3" empty with "No artifact chosen — the run uses what it finds in its own context."; a second example strip: "at the slot's bound" (Brand voice: greyed field, "2 of 2 artifacts chosen — remove one to choose another", two rows) and "no longer available" (Reference posts: Launch Announcement with a Deleted badge, Pricing Update with a Not visible here badge, both with a live remove control); the assistant example PERSONAL · Support Assistant with the Skills pane alone and no tab strip. | PASS |
| r2-sec-VII-per-agent-assignment-page-light-part2of2.png | The moved Skills drawings unchanged; the closing prose keeps its one sentence on the two caps and gains one paragraph on how the Artifacts pane differs from Skills and why. | The chosen-rows and degraded-rows drawings as before; the closing paragraph naming the Narrow column, the header, the Skills \| Artifacts strip, the assistant page, the two limits once, the Artifacts pane "drawn one group per declared manifest slot"; a new paragraph: the difference is the manifest — one group per slot, the same typeahead narrowed to the scope's artifacts of that kind, every choice by hand, no cap line of its own, the slot's minItems and maxItems the binding numbers, a slot with nothing chosen a normal state. | PASS |

Graded by the coordinating agent (the model named in the merge record) against the review of 2026-09-12 and PLAN: Per-Scope Skill and Artifact Assignment sections 1, 2, 5 and 6.2; 3 of 3 PASS. Open on the page: the plan's two bullets that say the page states both caps in its copy — the closing prose keeps that sentence once until the word on them.
