# design#153 — the Skills section moves to the per-agent assignment page (decision 358 A)

Three frames rendered from `specs/app-extensions.html` at the pull request's head 2a631bbb892b2a2fa2f384dc035092d6a49432fa in a headless browser (1440 css px wide, 2x device scale, light palette only — a design-spec change carries no dark-theme proof by the owner's rule of 2026-09-12). Whole sections, top to bottom, nothing cropped inside a section; section VII is two tiles because it is taller than one frame.

| file | requires | shows | verdict |
|---|---|---|---|
| sec-V-extension-settings-light.png | Section V keeps Permissions, Marketplace, Maintenance and the Danger zone (the access picker, vendor registration, publish gating, update, archive, delete) and no Skills group; one pointer sentence says skills and context artifacts are assigned per scope on the agent's assignment page (section VII), never on this admin page. | The settings-page mock carries Permissions, Marketplace, Maintenance (Update / Archive / Activate) and the red Danger zone (Reinstall latest, Force-delete); no Skills group anywhere on the page; the access-picker strip, the two vendor-registration strips, the update-state table, the archived pair, the force-delete and publish dialogs follow; the prose reads "Skills and context artifacts are assigned per scope on the agent's assignment page (§VII), never on this admin page." | PASS |
| sec-VII-per-agent-assignment-page-light-part1of2.png | A new section VII: the two address templates (an agent's `{scope-base}/agents/{vendor}/{slug}/settings?tab=skills\|artifacts`, an assistant's `…/assistants/…/settings?tab=skills`) and the five scope bases; a header naming the scope and the package; the Skills \| Artifacts tab strip; an assistant renders the Skills pane alone with no strip; the two limits stated on every assignment page ("5 per scope here, at most 5 effective per run."); the Artifacts pane named for context artifacts with a plain placeholder; the moved Skills sub-drawings (chooser narrowing; searching / no matches / search failed; five chosen with the chooser closed; a choice saving and a choice that failed) at the cap of five. | The address strip names both templates and the five bases /personal, /organizations/{id}, /teams/{id}, /projects/{id}, /workspace and ends "The scope comes from the address, not from a picker."; the agent example's header reads TEAM · GROWTH above Research Assistant · Agent by Cinatra with the address line; the Skills \| Artifacts strip with Skills active, then the limits sentence, the chooser, "2 of 5 skills chosen" and two chosen rows; the Artifacts pane example (Artifacts active, "Context artifacts", the placeholder "Its rows are drawn with the artifacts slice — this page adds no controls of its own."); the assistant example (PERSONAL · Support Assistant) with no tab strip, the limits sentence and "No skills chosen yet."; the chooser narrowing on "rese" with Active/Locked badges; the searching / no matches / search failed trio; "5 of 5 skills chosen — remove one to choose another" with the greyed field; the saving row and the failed-add message. | PASS |
| sec-VII-per-agent-assignment-page-light-part2of2.png | The sixth moved sub-drawing (chosen skills that have since degraded: Not installed / Archived / Role changed, each with a live remove control); the closing prose naming the Narrow column, the §II header with the scope above the package, the Skills \| Artifacts strip, the assistant's strip-less page, the two limits, and the Artifacts pane as the pane for context artifacts drawn by the artifacts slice. | The degraded rows with the three warning badges and the bin glyph on each; the prose paragraph with every named element; the footer "Application Design — Extensions v0.14.0". | PASS |

Graded by the coordinating agent (the model named in the merge record) against the plan sentences quoted in the pull request body and the drawing at the pull request head; 3 of 3 PASS.
