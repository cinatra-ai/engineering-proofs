# Pull request 3289 — live proof round five (2026-09-06)

Issue #3202: an Anthropic key held by the connection service resolves at the initial-sync seams; the headline re-proven at the forwarded head.

Frames, 1440x900 CSS at device scale 2, light and dark through the app's own theme control, the framework's development indicator off on pixels:

- `cell2-chat-turn-skill-selecting-light-assistant-answered.png` — the headline, light: a skill-selecting turn through the real provider settled with a written answer (120 s, the round's cold turn); five delivery rows read delivered.
- `cell2-chat-turn-skill-selecting-dark-attempt1-mcp-tool-listing-connection-closed.png` — the dark palette's first attempt: the provider returned a tool-listing connection error; kept under the name of what it shows.
- `cell2-chat-turn-skill-selecting-dark-assistant-answered.png` — the dark palette re-shot on a new conversation: the assistant answered (40 s).
- `cell1-llm-committed-claude-anthropic-{light,dark}.png` — the provider screen, reused unchanged from round one.

`measurements.json` and `dom-readings.jsonl` carry the readings taken at each shutter. The full record is the bot comment on the pull request.
