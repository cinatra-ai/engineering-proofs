# cinatra pull request 3048 — fourth capture (2026-08-30), graded

Twenty-two full-window frames (1440x900 at device scale 2, light and dark through the app's own theme control) of three real runs of the blog draft writer dispatched from the chat with the real provider, graded against the ratified drawing at the pin the pull request records and the six points of the change request. The two review-target cells graded DEFECT here were re-proved by the fifth set.

| picture | requires | shows | verdict |
|---|---|---|---|
| `v4-chat-thread-light.png` | the chip row draws one pill per skill, a checkbox in front of each label, the label reading the name then "by" the vendor in the secondary colour, and one Continue beneath the list — the row is the whole card. | the dispatch line, then four pills, each carrying three buttons (Confirm, Adjust, Skip), no checkbox in any pill, no "by {vendor}" half, no Continue anywhere. One recommendation root. | DEFECT — the chat host still draws the superseded Confirm/Adjust/Skip anatomy; the run page draws the ratified checkbox row, so the two hosts now draw two different cards for the same gate. One-root-per-surface passes. |
| `v4-chat-thread-dark.png` | the chip row draws one pill per skill, a checkbox in front of each label, the label reading the name then "by" the vendor in the secondary colour, and one Continue beneath the list — the row is the whole card. | the dispatch line, then four pills, each carrying three buttons (Confirm, Adjust, Skip), no checkbox in any pill, no "by {vendor}" half, no Continue anywhere. One recommendation root. | DEFECT — the chat host still draws the superseded Confirm/Adjust/Skip anatomy; the run page draws the ratified checkbox row, so the two hosts now draw two different cards for the same gate. One-root-per-surface passes. |
| `v4-skills-step-pending-light.png` | the Skills entry heads the rail with its glyph and no numeral; selecting it opens the checkbox chip row with one Continue. | rail row 1 is a glyph plus "Skills", no numeral; four checkbox pills, two ticked, one Continue beneath, no heading plate, no second card. | PASS. Note: this rail carries one row only, so "first of several" is not visible in this frame. |
| `v4-skills-step-pending-dark.png` | the Skills entry heads the rail with its glyph and no numeral; selecting it opens the checkbox chip row with one Continue. | rail row 1 is a glyph plus "Skills", no numeral; four checkbox pills, two ticked, one Continue beneath, no heading plate, no second card. | PASS. Note: this rail carries one row only, so "first of several" is not visible in this frame. |
| `v4-skills-step-settled-light.png` | once the run has started, the same pills draw read-only with no Continue. | one ticked pill, three clear, every box inert, no Continue anywhere, rail row now a filled check disc plus "Skills". | PASS. |
| `v4-skills-step-settled-dark.png` | once the run has started, the same pills draw read-only with no Continue. | one ticked pill, three clear, every box inert, no Continue anywhere, rail row now a filled check disc plus "Skills". | PASS. |
| `v4-setup-rail-light.png` | an answered Skills row is never drawn above the setup card, and two cards are never stacked in one detail. | rail one row (settled "Skills"); detail is the setup card alone; zero recommendation roots. | PASS on one-place. Rail again carries a single row, so no ordering is readable from this frame. |
| `v4-setup-rail-dark.png` | an answered Skills row is never drawn above the setup card, and two cards are never stacked in one detail. | rail one row (settled "Skills"); detail is the setup card alone; zero recommendation roots. | PASS on one-place. Rail again carries a single row, so no ordering is readable from this frame. |
| `v4-review-page-light.png` | the Skills entry is first on every run-page surface of a run that had a recommendation; the gate opens with the rail beside it; the representation slot renders the artifact through its type renderer and a target is never blank; decision bar in frame; no recommendation root in the detail column. | rail reads Skills then "1 Review" — Skills first, no numeral. Target header quotes the pinned revision and a build-time renderer chip. The representation slot holds one line: "No markdown is available to show for the revision being viewed." Decision bar present and fully in frame. Zero recommendation roots. | rail order and one-place PASS. DEFECT on the target — the panel claims a build-time renderer and renders nothing, which is neither the rendered artifact nor the floor's sanitized diagnostic. Also: the capture's own claim that there is "no first rendered heading to quote" is contradicted by these pixels — the first rendered heading is visible in the target header itself ("Planning a Connector Rollout That Teams Can Trust"); the capture's reader measured the expand-toggle control, not the target p |
| `v4-review-page-dark.png` | the Skills entry is first on every run-page surface of a run that had a recommendation; the gate opens with the rail beside it; the representation slot renders the artifact through its type renderer and a target is never blank; decision bar in frame; no recommendation root in the detail column. | rail reads Skills then "1 Review" — Skills first, no numeral. Target header quotes the pinned revision and a build-time renderer chip. The representation slot holds one line: "No markdown is available to show for the revision being viewed." Decision bar present and fully in frame. Zero recommendation roots. | rail order and one-place PASS. DEFECT on the target — the panel claims a build-time renderer and renders nothing, which is neither the rendered artifact nor the floor's sanitized diagnostic. Also: the capture's own claim that there is "no first rendered heading to quote" is contradicted by these pixels — the first rendered heading is visible in the target header itself ("Planning a Connector Rollout That Teams Can Trust"); the capture's reader measured the expand-toggle control, not the target p |
| `v4-review-page-target-open-light.png` | the representation slot, expanded, shows the pinned representation as the renderer would draw it. | a roughly 700px-tall panel that is entirely empty apart from its own "Collapse" control; rail, Skills entry and gate header are scrolled out of frame. | DEFECT — the expanded target is blank, and the frame carries none of the required readings. |
| `v4-review-page-target-open-dark.png` | the representation slot, expanded, shows the pinned representation as the renderer would draw it. | a roughly 700px-tall panel that is entirely empty apart from its own "Collapse" control; rail, Skills entry and gate header are scrolled out of frame. | DEFECT — the expanded target is blank, and the frame carries none of the required readings. |
| `v4-run-page-rail-light.png` | the recommendation entry stays first on the rail; a gate is a step in the run and opens under the same rail; the paused step is highlighted. | three rows in order — Skills (no numeral), "Step 1", Review (gate glyph). Skills is first. Detail carries the review gate with the same blank representation line. Zero recommendation roots. | PASS on Skills-first and one-card-per-detail. Same blank-target DEFECT as above. Secondary note: the work step is labelled generically "Step 1" where the drawing names rail entries after what the step did. |
| `v4-run-page-rail-dark.png` | the recommendation entry stays first on the rail; a gate is a step in the run and opens under the same rail; the paused step is highlighted. | three rows in order — Skills (no numeral), "Step 1", Review (gate glyph). Skills is first. Detail carries the review gate with the same blank representation line. Zero recommendation roots. | PASS on Skills-first and one-card-per-detail. Same blank-target DEFECT as above. Secondary note: the work step is labelled generically "Step 1" where the drawing names rail entries after what the step did. |
| `v4-run-page-skills-selected-light.png` | selecting the recommendation entry opens the row without a second instance; the opened step is highlighted; a started run's pills are read-only with no Continue. | same three-row rail; detail holds the four pills alone, one ticked, all inert, no Continue, one root. The Skills label and the pending Review row draw with identical emphasis — pixel-compared, only the Skills label's weight differs from the closed state, and it matches the Review row's own weight. | PASS on one-place and the read-only row. DEFECT (minor) — the rail carries no distinct selected state; a reader cannot tell from the rail alone which step is open. |
| `v4-run-page-skills-selected-dark.png` | selecting the recommendation entry opens the row without a second instance; the opened step is highlighted; a started run's pills are read-only with no Continue. | same three-row rail; detail holds the four pills alone, one ticked, all inert, no Continue, one root. The Skills label and the pending Review row draw with identical emphasis — pixel-compared, only the Skills label's weight differs from the closed state, and it matches the Review row's own weight. | PASS on one-place and the read-only row. DEFECT (minor) — the rail carries no distinct selected state; a reader cannot tell from the rail alone which step is open. |
| `v4-schedule-rail-light.png` | on the setup/schedule surface, the rail reads Skills first, then "1 Schedule", then "2 Review". | a single row, "1 Schedule"; no Skills entry, no Review entry; tabs read Setup / Schedule / Permissions. Detail: the schedule card alone, "Estimated run duration / Unavailable." (tracked at pull request 3072, not re-graded here). Zero recommendation roots. | NOT PROVEN — the required three-row order is absent from this frame; the run photographed never held a Skills question and has no review gate. Page separation (Skills never drawn on the schedule page) passes. |
| `v4-schedule-rail-dark.png` | on the setup/schedule surface, the rail reads Skills first, then "1 Schedule", then "2 Review". | a single row, "1 Schedule"; no Skills entry, no Review entry; tabs read Setup / Schedule / Permissions. Detail: the schedule card alone, "Estimated run duration / Unavailable." (tracked at pull request 3072, not re-graded here). Zero recommendation roots. | NOT PROVEN — the required three-row order is absent from this frame; the run photographed never held a Skills question and has no review gate. Page separation (Skills never drawn on the schedule page) passes. |
| `v4-schedule-surface-light.png` | (see the graded record) | Pixel-diffed against the schedule-rail frames of the same theme: differs only inside the top notification-badge region (bounding box roughly y 31–94 light, y 31–68 dark). Same single "1 Schedule" row, same schedule card. Verdict: NO NEW EVIDENCE — a duplicate view; the required order is still not shown. | NO NEW EVIDENCE — a duplicate view; the required order is still not shown. |
| `v4-schedule-surface-dark.png` | (see the graded record) | Pixel-diffed against the schedule-rail frames of the same theme: differs only inside the top notification-badge region (bounding box roughly y 31–94 light, y 31–68 dark). Same single "1 Schedule" row, same schedule card. Verdict: NO NEW EVIDENCE — a duplicate view; the required order is still not shown. | NO NEW EVIDENCE — a duplicate view; the required order is still not shown. |
| `v4-full-rail-light.png` | (see the graded record) | rail carries one row, a glyph plus "Skills"; detail holds four checkbox pills, one ticked, one Continue. | PASS as a pending-Skills reading. The file name asserts a "full rail" the pixels do not show — a single-row rail, not the three-row surface the name implies. |
| `v4-full-rail-dark.png` | (see the graded record) | rail carries one row, a glyph plus "Skills"; detail holds four checkbox pills, one ticked, one Continue. | PASS as a pending-Skills reading. The file name asserts a "full rail" the pixels do not show — a single-row rail, not the three-row surface the name implies. |
