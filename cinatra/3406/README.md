# Issue #2702 — proof round 5

The extension settings screen without the Skills section, at the head
316a8c8dafdd2d1684562de065dc42a2af4869a3 of pull request #3406.

Every frame below is a real page of the product, reached through the product's
own navigation: the topbar Configuration control, then Extensions — Installed,
then the installed card's Settings entry. Each frame carries that page's own
breadcrumb — Configuration / More / @Cinatra Ai / Web Research Agent — at the
page URL /configuration/extensions/settings/agent/@cinatra-ai/web-research-agent.
No frame comes from the pixel suite's harness.

Frames are 1440 by 900 at a device pixel ratio of 2, in both palettes, the
palette switched through the product's own theme control. The bottom band of
the sidebar is painted over. The framework's own development indicator is
absent from every frame.

## Frames

| file | cell | palette | scope |
| --- | --- | --- | --- |
| cell1-extension-settings-screen-web-research-agent-light-window.png | CELL1 | light | window |
| cell1-extension-settings-screen-web-research-agent-light-fullpage.png | CELL1 | light | whole page |
| cell2-extension-settings-screen-web-research-agent-dark-window.png | CELL2 | dark | window |
| cell2-extension-settings-screen-web-research-agent-dark-fullpage.png | CELL2 | dark | whole page |

## Readings

The settings screen's sections, in document order: the extension header, then
Permissions, Execution, Marketplace, Maintenance, and the Danger zone. There is
no Skills heading, no Skills row and no Skills control on the screen, and the
screen's own text carries no skill word at all. The one match of that word in
the window belongs to the application sidebar's own global navigation entry,
which is not part of this screen.

The Execution section and the header publisher line are pre-existing filed
departures: recorded here, not counted against the round.

The wrench in the topbar is the development-boot road's own artefact and is not
counted against any cell.

## CELL3 — not shot

The read-only settings view was not shot. At this head no page of the product
draws such a view: the presentational settings view is mounted by the settings
screen itself, and apart from that only by the pixel suite's harness, which is
never a surface. The cell is reported as not shot rather than stood in for.

Full per-frame readings are in dom-readings.jsonl; file measurements are in
measurements.json.
