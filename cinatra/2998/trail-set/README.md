# cinatra pull request 2998 — trail proof round (2026-09-01)

Issue 2934. Head `93152e9541ab`. Full record: pull request 2998, comment 5496019776.

Four frames, full window 1440 by 900 at device scale 2, both colour themes, every frame at rest.
One real run under a real provider, one neutral fixture person.

- `vt-trail-run-page-light.png` / `-dark.png` — the run's own page: trail `Agents > Blog Pipeline Agent (1)`, crumb row left edge at the sidebar's inner edge plus the standard gutter (32). The tab read `Agent | Cinatra` here — the trail was named and the tab was not, which is the gap the later title round closed.
- `vt-trail-chrome-composite-light.png` / `-dark.png` — the agents list: trail `Run agent`, tab `Agents | Cinatra`, the same 32 gutter, the right-hand cluster flush at 1408.

What this round left owed, stated in its own record: the review page was not reached, because no
run it could dispatch opened a review gate; and the tab did not yet mirror the trail on the run's
own page. Both are closed by the title round in the sibling folder.
