# cinatra pull request 2998 — crumb proof round (2026-09-01)

Issue 2934. Head `93152e9541ab`. Full record: pull request 2998, comment 5496247524.

Nine frames, full window 1440 by 900 at device scale 2, both colour themes. This round graded the
trail on unresolvable and substitute addresses; its own record grades most of these frames as
failing on one item, and they are published as the reading they actually are, not as passes.

- `vc-name-cleared-on-missing-page-light.png` / `-dark.png` — PASS, 14 of 14: entry from a tab that had just resolved a name; the name is cleared and the trail reads `Agents > Agent run`.
- `vc-missing-page-crumb-light.png` / `-dark.png` — 12 of 13: the trail on an address that resolves to nothing. The one failing item is the tab reading on a cold typed entry, which the later title round closed.
- `vc-other-reader-not-found-crumb-light.png` / `-dark.png` / `-light-remeasured.png` — 12 of 13, same single failing item: a second reader's not-found reading. The remeasured light frame corrects a measurement of the wrong node in its twin.
- `vc-resolvable-page-crumb-light.png` / `-dark.png` — failing as a proof cell: a project page stands in for a resolvable id-bearing agent page, because no run existed in that instance to make one; the sidebar identity also renders empty here. Published as the substitution it is.

The record for this round also states the two subjects it could not construct through the
application's own forms, and corrects one claim of its own summary rather than repeating it.
