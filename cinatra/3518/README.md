# Proof round 1 — the run page rail's entries on one left edge (cinatra#3518)

Real run pages on a development boot at the pull request's head 98ea37640c71, one real run of the Blog Idea Generator Agent started through the product's own Run road.

- `cell1-parked-light.png`, `cell1-parked-dark.png` — requires: a run parked at its review step. Shows: the rail's four entries (Schedule, Setup, Review, What this run made) with every row and every circle on one left edge (464 px, one distinct value each). Verdict: pass in both palettes. Recorded, not counted: the run detail's plate is still empty at the shutter (the review content draws late — the known slow review island).
- `cell2b-finished-light.png`, `cell2b-finished-dark.png` — requires: the same run finished. Shows: the same four entries, the resolved review entry included, all on the same left edge. Verdict: pass in both palettes. Recorded, not counted: the detail's own words and the doubled scope word on each item belong to other open pull requests.
