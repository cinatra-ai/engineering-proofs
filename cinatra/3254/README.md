# Pull request 3254 — third proof round (2026-09-06)

Every picture: the run page's Skills step on a dev boot, both palettes, at proof head 71bf5290; graded against design main 033a697c on the issue's own cells (#3184). The `-onerow` twins are the same cells on ONE run row per palette (cells 1 to 4 on the same row), the others on further rows as independent confirmation. Each line states what the drawing requires and what the picture shows.

- `c3-cell1-skills-step-before-any-choice-*` — requires: the step rail names the run's ordered steps; no tab is drawn selected for a step shown inside the frame; no heading plate above the row. Shows: rail Skills · 1 Setup · 2 Schedule · 3 Review, no tab lit, the row and its Continue as the whole card.
- `c3-cell2-skills-step-one-skill-ticked-*` — requires: the same with one skill chosen. Shows: one box checked; rail, strip and card unchanged.
- `c3-cell3-after-continue-rail-four-entries-no-tab-lit-*` — requires: after Continue, on the same page without a reload, the rail still names every step and no tab is lit while the Setup step stands in the frame. Shows: four entries, no tab lit (the one counted departure: no entry carries the current marker in that instant — filed as its own issue).
- `c3-cell4-settled-skills-entry-reopened-after-continue-*` — requires: the settled Skills entry re-opened right after Continue keeps the four-entry rail and no lit tab. Shows: exactly that.
- `c3-cell4b-settled-skills-entry-reopened-fresh-load-*` — requires: the same after a fresh load. Shows: exactly that.
- `c3-cell5-next-step-own-frame-fresh-load-*` — requires: the next step's own frame on a fresh load: Skills settled above, 1 Setup carrying the current marker, no tab lit. Shows: exactly that.

Recorded, not counted (pre-existing on the main line): the "No messages yet." placeholder (#3222), the third breadcrumb crumb (#3223), the etched-rule token (#3142), the "Unknown vendor" pills, the read-only re-opened row, and the shell chrome disposed by the standing decision.
