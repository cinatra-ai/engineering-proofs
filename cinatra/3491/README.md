# Proof round 4 — the review prompt window on the two surfaces, at 9937d6b56856ba445ea6b7e9b8bc62e52a1f007f

Both halves of the claim under pull request 3491 are re-shot at this head: inside a
conversation no review prompt window is drawn and the pending review card now draws, and
the run page carries exactly one review prompt window, in the page chrome.

## The two real runs behind the frames

- CELL1 and CELL2 stand on one real run of the blog idea generator agent 0.2.0,
  dispatched in a conversation: a new thread, one message naming the agent and giving it
  its brief, sent from the thread's own composer. The assistant started the run, the run
  asked for its brief and its schedule on its own screens in the thread, and it reached
  its artifact review gate in the same turn. Run 94855f7a-2aaf-46aa-9818-42b738532718,
  runtime task 6e009e89-3ce4-41fe-b556-3001a0768327, trigger immediate.
- CELL3 stands on its own real run, dispatched from the agent's own Run entry:
  Agents in the sidebar, the Blog Idea Generator Agent, Run agent. Run
  0bd036c3-07c6-4553-8d41-1c02ff289f46, runtime task f5545f99-911a-4523-abeb-213afd04e8dc,
  trigger immediate. Its frame is taken on the run's own page while the current step
  screen holds input a person can work with.

## What the frames show

| file | what it is |
| --- | --- |
| `cell1-chat-thread-zero-review-prompt-windows-one-composer-light.png` | the conversation at page level, light |
| `cell1-chat-thread-zero-review-prompt-windows-one-composer-dark.png` | the same, dark |
| `cell2-chat-host-pending-review-card-drawn-light.png` | the pending review card drawn whole in the thread, light |
| `cell2-chat-host-pending-review-card-drawn-dark.png` | the same, dark |
| `cell3-run-page-one-review-prompt-window-in-the-page-chrome-light.png` | the run page, its one window in the page chrome, light |
| `cell3-run-page-one-review-prompt-window-in-the-page-chrome-dark.png` | the same, dark |
| `dom-readings.jsonl` | one reading per shutter, written in the same instant |
| `measurements.json` | per file: sha256, bytes, pixels, mean luminance, the counted region and its luminance |
| `chat-final-reading.json` | the conversation's reading after the four chat shutters |
| `run-page-reading.json` | the run page's reading at its shutters |

On the chat frames the page carries zero nodes with the review prompt window anchor, the
review card's own root carries none either and no send control of one, the thread's own
composer stands exactly once at the foot, and the card's own root is laid out 686 by 940
with no ancestor computing display none — the reading the previous round took forty times
and found at zero by zero. The decision floor's own note field is present inside the card
and is named as the floor's, never as a prompt window.

On the run frames exactly one review prompt window is drawn, its ancestor chain reaches
the run page's own window container, and every lifecycle screen and card on the page
carries zero window anchors.

Both palettes are reached through the product's own theme control at the same instant of
the same run. The account band is painted over in every frame. The framework's own
development indicator was switched off through its own preference and drew zero elements
at every shutter. The small wrench in the top bar is the development build's own control
and belongs to the road this round ran on, not to any cell.

## Read and not counted

The artifact preview inside the review card reports that it did not load and offers to
try again; the decision floor beneath it stands complete. The card's composition, the
review composition and the decision floor are pre-existing and filed, so they are read
and recorded here, never graded.
