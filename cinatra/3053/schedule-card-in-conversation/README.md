# cinatra pull request 3053 — criterion 6 of #3044: the schedule card reaches the conversation on a real run

Real runs through the app's own dispatch and the real provider; full browser window at 1440x900, device scale 2; light and dark through the app's own theme control; the person drawn in the sidebar footer; every frame taken at rest.

| picture | luminance | requires | shows | verdict |
|---|---|---|---|---|
| `00-setup-secrets-envmanaged-light.png` | 238.87 (min 32, max 255) | THE TASK's own words: after the lane's Nango containers are up and `node scripts/gen-nango-env.mjs` has filled the key, "open /setup/secrets — env-managed, asking nothing — one frame (light) as the readback". Full window 1440x900 at device scale 2, light, no development pill. The ratified drawing fixes nothing on this screen (it draws lifecycle cards only), so it is graded against the task's words | Verified independently: 2880x1800 (1440x900 @2), sha256 e32a2785…6be9f1, 128781 bytes, mean luminance 238.87 (min 32, max 255) — inside the light band. The Setup shell with the step rail ACCOUNT · KEY · NAME · SECRETS · MODEL, every pill ticked (so this is a revisit of an already-completed step, not the first pass). Heading "Secrets"; the lede naming Nango. Secret key: an empty, live input with "L | PASS. The step asks for no value: both fields state that blank keeps what the environment already set, and the step is passable on Continue alone. Stated precisely and not softened: this is "env-managed" in the sense of no value being required, not a read-only env banner — both fields are still live, editable inputs. T |
| `01-pending-light.png` | 239.21 (min 31, max 255) | Drawing §VI, the first-shown reading: "the question When should this run? over the three option rows, the chosen row taking the indigo edge and tint and owning its fields, and the estimated duration beneath"; rows editable, floor Confirm, "It is the only control: there is no Adjust"; "No summary box is ever drawn, no status label"; "There is no raw cron field"; "One card, five readings, and never  | 2880x1800, sha256 34c81efe…c35fd36, 219718 bytes, mean luminance 239.21 — light band. Sidebar with the person in the footer (Ada Lindqvist, ada.lindqvist@example.c…). Header: Chat › the thread title, theme control drawn as the sun. User turn right-aligned with name + AL initials, filled bubble, copy and pencil marks beneath. Assistant turn: Cinatra mark and name, then one sentence, then EXACTLY ON | PASS on every element the drawing fixes for the first-shown reading — one card, the question, three editable rows, the chosen row on the drawing's exact indigo edge and tint, the duration region beneath, Confirm as the only control, no Adjust, no summary, no status label, no cron field, no second card, the card in the  |
| `01-pending-dark.png` | 16.18 (min 4, max 255) | The same §VI/§I/§IX requirements as the light frame, reached only through the app's own theme control, with a mean luminance inside the dark band (below 128/255). The ratified drawing carries NO dark rendering — its only palette is the light one (--blue #364e81, --line-strong #15213a, --surface-strong #ffffff), so the dark frame is graded on structure, state and controls, and on any colour the dra | 2880x1800, sha256 cfab52fd…52013bc23, 219703 bytes, mean luminance 16.18 (min 4, max 255) — deep inside the dark band. The theme control is drawn as the moon, so dark was reached through the app's own control. Structure identical to the light frame on every count: one card in the content slot, the question, three rows with Run right after setup chosen, the duration region, one hairline, one Confir | PASS on structure, state and controls — the reading is the drawing's first-shown reading in every region, and the dark band is met with room to spare. DEFECT: the chosen row does not take "the indigo edge and tint" the drawing fixes — it is drawn with a near-white edge (#e2e8f0) and a hueless tint, so the one colour §V |
| `02-confirmed-light.png` | 239.01 (min 0, max 255) | Drawing §VI, the configured reading: "Confirmed, the card stays exactly where it is. No second card is drawn and nothing is summarised: the same rows show the schedule as it stands, and the floor becomes Save changes — quiet until a row is actually changed"; rows still editable; "Read the two examples above against each other and only one thing differs: the word on the floor"; "Cancel schedule app | 2880x1800, sha256 649ee23d…4373efb32db, 235168 bytes, mean luminance 239.01 — light band. The card is in the same place, the same size, in the same content slot; exactly one card. "Schedule for later" is now the chosen row, on the drawing's indigo edge and tint, and it owns its two fields: "Run at 29.08.2026, 10:18" (a live datetime control with its calendar glyph) and "Timezone Europe/Berlin" — t | PASS — this is the drawing's configured reading exactly: the same card in the same place, the same rows still editable, no second card, nothing summarised, and only the word on the floor changed, quiet because nothing has been changed yet. Cancel schedule is correctly absent (this is a one-off, not recurring). The stan |
| `02-confirmed-dark.png` | 17.26 (min 4, max 255) | The same configured reading as the light frame, in dark through the app's own theme control, mean luminance below 128. | 2880x1800, sha256 945fe4bf…3f65bd2a4, 234976 bytes, mean luminance 17.26 — dark band. Moon control. Identical reading to the light frame: one card in place, Schedule for later chosen and owning Run at 29.08.2026, 10:18 and Timezone Europe/Berlin, the other rows drawing no fields, rows editable, the duration region, one hairline, one quiet Save changes and nothing else on the floor. No Confirm, no  | PASS on structure, state and controls — the configured reading is complete and the dark band is met. The same DEFECT as 01-dark: the chosen row's accent is the theme's near-white dark --primary, not the indigo edge and tint §VI fixes, and the drawing has no dark rendering to license it. The "The run started." contradic |
| `03-reload-light.png` | 239.21 (min 0, max 255) | The task's words: "the same card after a reload, still there, drawn from the turn's durable content". Drawing §VI: the card is drawn once and stays where it is; the configured reading keeps editable rows over Save changes; never a second card. | 2880x1800, sha256 6d9c3b8b…c6f855508, 236016 bytes, mean luminance 239.21 — light band. A distinct render (different bytes and hash from 02-light), and the card reading is unchanged: one card in the same place, Schedule for later chosen on the indigo edge and tint, Run at 29.08.2026, 10:18 and Timezone Europe/Berlin still stated, rows editable, duration region, one quiet Save changes, no second ca | PASS for the requirement under test — the card survives the reload whole, in place, with both stated values and the same floor, so it is drawn from the turn's durable content and not from live state. Two things stated rather than softened: (a) the pixels alone cannot prove a reload occurred — the card region is unchang |
| `03-reload-dark.png` | 17.26 (min 4, max 255) | The same post-reload reading, in dark, mean luminance below 128. | 2880x1800, sha256 fdbdf844…a5d32e5e0, 234920 bytes, mean luminance 17.26 — dark band. The reading is complete and unchanged: one card, Schedule for later chosen with both values, rows editable, quiet Save changes, no second card, no summary, no status label. A pixel diff against 02-dark returns a bounding box of 110x6 px inside the Cinatra wordmark — text antialiasing only; every other pixel of th | PASS for the requirement under test: the card is still there after the reload with both stated values and the same floor. Stated plainly: this frame differs from the pre-reload dark frame by 110x6 px of wordmark antialiasing and nothing else, which is exactly what "unchanged across a reload" should look like but also m |
| `04-immediate-reading-light.png` | 238.65 (min 31, max 255) | The task's words: "also the 'Run right after setup' reading where the recipe names it" — a positive reading in which no schedule card is minted and the run's own next reading stands in its place, taken without a reload. Drawing §VI: nothing exists until the reader confirms, so where the run goes immediately there is no proposal and no card. Drawing §I: "A card takes that content slot, at the colum | 2880x1800, sha256 11575ca8…e88f312a3c, 225289 bytes, mean luminance 238.65 — light band. A different run is named in the sentence (3f6b5843-65dc-408b-b7b8-b1500f7ac7bc), and here "The run started." is accurate. NO schedule card anywhere: no "When should this run?", no option rows, no Confirm. In the assistant's content slot, TWO panels are stacked: first a white card holding an "Idea Context" pane | PASS for the requirement under test — this is a positive reading, not merely an absence: the schedule card is gone and the run's own next gate is drawn in its place, on the untouched page. DEFECT, stated plainly: two panels are stacked in one assistant content slot where §I draws a single card taking that slot, and the |
| `04-immediate-reading-dark.png` | 14.10 (min 4, max 255) | The same positive 'Run right after setup' reading, in dark through the app's own theme control, mean luminance below 128. | 2880x1800, sha256 9ae6459b…12607330b957, 226580 bytes, mean luminance 14.10 (min 4, max 255) — the darkest of the set, well inside the band. Moon control. The same reading as the light frame: no schedule card, the Idea Context gate with its single Continue, and the stacked Agentic Run Progress panel with the "pending approval" pill and "No messages yet." No development pill, no skeleton, no stale  | PASS for the requirement under test. The same DEFECT as its light twin: two stacked panels in the one content slot §I draws a single card into, and both panels are surfaces the ratified page does not draw at all. |

## Findings, as measured

- The turn that introduces the card contradicts the card, in 01, 02 and 03, light and dark: the assistant's sentence reads "Dispatched @cinatra-ai/blog-idea-generator-agent (runId: 2315b02a-cda3-488c-ad1f-a634dce702b6, status: queued). The run started." while the card directly beneath is still asking "When should this run?" and offering Confirm, and the run row reads pending_trigger. The drawing's first-shown reading is "nothing exists yet" and its own example sentence introduces the card as a proposal to confirm; here the reader is told the run has already started and simultaneously asked to schedule it. It persists unchanged after Confirm and after the reload, where it also disagrees with the card's own 10:18 value.
- Dark theme, all four dark card frames: the chosen option row does not take "the indigo edge and tint" §VI fixes. Sampled, it is a near-white edge (226,232,240) with a hueless fill lift (24,34,51 over a 13,24,42 ground) — the app's dark --primary (.dark maps --primary to --accent in globals.css). The ratified drawing has no dark rendering at all, so nothing in it licenses the substitution: in dark, the one colour the drawing names for the chosen row is absent. The mapping is app-wide and is not introduced by this pull request.
- the lane's host input's edge is unstable across frames of the same reading: the chat box is drawn at exactly #15213a (the drawing's line-strong) in 02-confirmed-light, and at a pale #dee0e3 hairline in 01-pending-light, 03-reload-light and 04-light. §I draws the chat box as the lane's host input carrying the line-strong edge — "the darker-edged one on the page" — while also stating the hierarchy, not the look, is what is fixed; the inconsistency is visible between two frames that must otherwise read the same (02-light vs 03-light differ substantively nowhere else).
- Frame 04, both themes: two panels are stacked in one assistant content slot — the Idea Context gate with its Continue, and an Agentic Run Progress panel carrying a "pending approval" status pill — where §I draws a single card taking that slot at the column's full width. Neither panel is drawn on any section of the ratified page, so their chrome, floor and status pill have no drawing to be graded against.
- The estimate region reads "Estimated run duration / Unavailable." in every pictured schedule reading (01, 02, 03, both themes). The region is present and correctly placed beneath the rows, but the drawing draws a value there in every one of its five readings and never draws an unavailable estimate.

**Overall:** All nine frames are genuine, full-window 1440x900 at device scale 2 with hashes, bytes and luminances I recomputed and confirmed exactly (light 238.65-239.21, dark 14.10-17.26, every dark frame far inside the band), and the schedule card itself conforms to the ratified drawing's §VI in light on every reading — one card in the assistant's content slot, the question over three editable rows with the chosen row on the drawing's exact #364e81 edge and 5% tint, the duration region beneath, Confirm as the only control and then a quiet Save changes, no Adjust, no cron field, no summary box, no status label, no second card, unchanged across the reload, and correctly absent in the immediate reading; the defects are the turn's \"The run started.\" contradicting the card it introduces, the dark chosen-row accent replacing the drawing's indigo with a near-white the drawing never licenses, an unstable chat-box edge, 04's two undrawn panels stacked in one drawn slot, and an estimate region that draws no estimate.
