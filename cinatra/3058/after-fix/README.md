# cinatra pull request 3058 — the review inside a third-party application's widget, after the fix for the pending target panel

Full browser window of the third-party host page, 1440×900 at device scale factor 2, light and dark through the app's own theme control; one real run. The change request's substance is proved (the pending card draws the artifact's header and rendered body at the pending instant, one slot on the widget host, three affordances over one rationale field, decided on the card's own bar, settling in place and surviving a reload); the set is thinner than its names say and carries defects, each stated in the table and below. Graded against the ratified drawing at the contract's pin and the pull request's change request.

| picture | sha256 | requires | shows | verdict |
|---|---|---|---|---|
| F2-review-card-pending-target-panel-site_widget-light.png | `151f245f3e48` | Cards §II: the placeholder is replaced, in place, by the review — the same slot, in the same turn, on its own; the card carries no link to the run page. Cards §IX: the review card reads Yes on the site widget and is the same card there — same regions, same states, same data, actions live for a reader who may act. Cards §IV pending. Review drawing §IV: an immutable header naming | sha256 151f245f3e48471eb85857d7e0d8b3122f3add1a28e4ca26cfc88c5a298614f8, 2880x1800 (1440x900 at scale 2), full host-page window uncropped, mean luminance 240.4 whole / 238.2 widget region (light band). One slot ([data-run-review-slot] = 1, value review), one review card, cardHostAttr site_widget, one site-widget host declaration, state pending, placeholders 0, run-page links 0, | PASS on the delivery the change request asked for — the header and the rendered body are drawn at the pending instant in the widget, in one slot, on the site-widget host, with the three affordances over one rationale field, and the failed-preview notice is gone. DEFECT: the renderer provenance of re |
| F2-review-card-pending-target-panel-site_widget-dark.png | `79d4cdf73501` | The same as the light cell, in the palette the reader chose on the app's own theme control: the widget region measured under 128/255, dark from a class the app sets and never from operating-system colour-scheme emulation. | sha256 79d4cdf735017ee6f66f5ccfdbc570999716bc18a4543dd77a61480f4e84727e, 2880x1800, mean luminance 159 whole / 30 widget region. The whole-frame 159 is the third-party host page's own light styling around the widget, disclosed; the widget region is 30, well under the band. themeClass ends in 'dark', osPrefersDark false, context opened with the light operating-system preference  | PASS as a picture of the pending card in dark — the palette is real, the header and body are drawn, the floor is drawn, one card in one slot on the site-widget host. Carries the same three drawing defects as the light cell (no renderer provenance; row facts and updated time not as drawn; the recipe' |
| F3-decision-on-the-cards-own-bar-site_widget-light.png | `151f245f3e48` | The brief's frame 3: the decision taken on the card's own bar inside the frame, under the widget's own credential, with the resolve and decide requests carrying the broker headers and no cookie. Cards §X: while the act is in flight the card takes its loading reading, then settles in place. | sha256 151f245f3e48471eb85857d7e0d8b3122f3add1a28e4ca26cfc88c5a298614f8 — byte-identical to the F2 light frame. The shutter fired at 03:08:31.993; Approve was pressed at 03:08:33.762, 1.77 s later. The pixels are the pending card with its Comment / Reject / Approve bar; nothing in them shows a press, an in-flight reading, or a settled one. The record files it as a separate shot | DEFECT: the frame named 'decision on the card's own bar' is the pending frame — same bytes as F2, shot before the press. It evidences the bar in view, not the decision, and it adds no reading F2 does not already carry. The record compounds this by asserting a separate shutter instant and separate sa |
| F3-decision-on-the-cards-own-bar-site_widget-dark.png | `79d4cdf73501` | The same as the light F3 cell, in dark. | sha256 79d4cdf735017ee6f66f5ccfdbc570999716bc18a4543dd77a61480f4e84727e — byte-identical to F2 dark and to the DISCARDED dark frame. Shutter recorded at 03:08:33.759, one millisecond before the press at 03:08:33.762. Luminance 159 whole / 30 widget. State reads pending, decision bar present. The Approve was pressed in the light context; every brokered decide request in the reco | DEFECT: the file names a decision that was not taken in this context and shows no decision in any case — it is the dark pending frame, byte-identical to F2 dark, shot one millisecond before a press made in the other context. It carries no evidence of its own. |
| F4-review-card-settled-in-place-site_widget-light.png | `8fd678610ab7` | Cards §X: the card settles in place, in the same slot, in its own settled reading. The settled panel as drawn — a marked square over a semibold line and a muted sentence — naming the decider where the name is safely displayable. Nothing left to press; no run-page link; the transcript unmoved. Review §IV's immutable header still fixed in place. | sha256 8fd678610ab794e1960dbef93a7827acb13d9a5692fb93ec3489c75a24ed35eb, 2880x1800, luminance 240.7 whole / 238.9 widget. Same slot ([data-run-review-slot] = 1, review), one review card, host site_widget, state settled, decision bars 0, Approve/Reject/Comment 0, pressables in slot 1 (the target's Expand), run-page links 0. The header and the rendered body are unchanged and stil | PASS — the settled reading in place, in the same slot, in the same page instance, with the decider named and no affordance left, and the header still fixed above it. Carries forward the §V renderer-provenance defect and the header row-fact rendering deviation. This frame and F5 dark are the whole of |
| F5-review-card-settled-after-reload-site_widget-dark.png | `3e294d1847db` | The brief's frame 5: after the third-party page is reloaded and the widget signs in again through its own hosted flow, the same settled card is still there — same slot, same state, nothing to press. | sha256 3e294d1847dbef3eb3a9142450da13b74fe19642a1bd9b071f5cc6e11e1881b8, 2880x1800, luminance 158.6 whole / 29.1 widget region, themeClass dark, osPrefersDark false. One slot at review, one review card, host site_widget, state settled, decision bars 0, pressables 1, run-page links 0, 2 transcript blocks. The header, the rendered body and the 'Approved by Ops Operator Lane / The | PASS on the clause — the settled card is still there after a real reload and a fresh hosted sign-in, in dark, in the same slot with nothing to press. DEFECT of the stated mechanism, carried over from the earlier round and unchanged: zero stored turns carry the gate part, so the persistence is a proj |
| DISCARDED-F4-dark-column-still-pending-at-the-shutter-site_widget-dark.png | `79d4cdf73501` | Graded as the capture's own rejected shot: it is not part of the set and is not offered as proof of anything. The reason for the discard must hold on inspection. | sha256 79d4cdf735017ee6f66f5ccfdbc570999716bc18a4543dd77a61480f4e84727e — byte-identical to F2 dark and F3 dark. Shutter recorded at 03:12:42.274, about four minutes after the Approve landed (gate resolved_at 03:08:34.679); the same-instant readings say state pending with the decision bar still drawn, and the pixels are the pending card. The discard reason therefore holds: this | Correctly DISCARDED and correctly named — it is a pending card, not a settled one, and it is no part of the set. Two things must still be said about it: it is the same image the capture filed as F2 dark and F3 dark, so one dark image stands under three names in the record; and its existence is the p |

Defects, stated plainly:
- F3 light and F3 dark are byte-identical to F2 light and F2 dark (151f245f3e48471e… and 79d4cdf735017ee6…). The frames named 'decision on the card's own bar' are the pending frames, shot 1.77 s (light) and 1 ms (dark) BEFORE the Approve press. No frame in the set shows the decision being taken, and none shows the card's in-flight loading reading that cards §X draws. The decision is proved by the brokered decide, the gate row and F4 light — not by a picture.
- One dark image (79d4cdf735017ee6…) is filed under three names — F2 dark, F3 dark, and the DISCARDED F4-dark — while measurements.json records three distinct shutter instants (03:08:29.642, 03:08:33.759, 03:12:42.274) and three distinct 'same instant' DOM readings for it, and states no identity between the files. The earlier round's body declared the F2/F3 identity openly; this record does not.
- The dark column never re-read the decision: it was pixel-for-pixel frozen from 03:08:29 to 03:12:42 and still read state=pending four minutes after the gate resolved. There is therefore NO settled-in-place frame in dark. The settled-in-place reading is pictured in light only (F4 light); the dark settled reading exists only after the reload (F5 dark). Against the recipe's 'each state light AND dark' that is one missing cell: settled-in-place x dark.
- The premise that the light F5 frame is absent is FALSE. F5-review-card-settled-after-reload-site_widget-light.png exists in the capture's cells directory (sha256 2ccbb3fa0ba423d4a9867c88cf6b3c46ca3c923e54df803363f37bfb40f2c6, 370508 bytes, distinct from F4 light), and on inspection it is a valid settled-after-reload light frame — same slot, state settled, header and body drawn, decider named, nothing to press. It was omitted from the list handed for grading, not from the capture. There is no 
- Review drawing §V is not satisfied on any frame, either palette, pending or settled: the representation slot is labelled only 'RENDERED'. It names no renderer, no build-time/runtime provenance and no slot, where the drawing draws a provenance chip ('Outreach type renderer · detail', 'Email build-time · detail') and states that each target says how it was rendered.
- The header's row facts do not render as the drawing draws them: the card writes 'Ownership: organization · Visibility: organization' and a raw ISO instant 'updated 2026-08-29T03:07:18.778Z' where the drawing writes bare scope words ('Team · Private') and a relative time ('updated 8 min ago'). Every fact §IV requires is present; the drawn line is not matched.
- The re-shoot recipe's point-4 readings are not evidenced on any frame: targetHeaders 0, targetHeaderText empty, targetFloors 0, targetFloorPackage null, and the island document's own header and body anchors read 0 — while islandLoadState reads 'loaded' and renderedChars 14539, and the pixels plainly show both header and body. The record does not settle whether the observer's selectors missed inside the island frame or the anchors the fix introduced are absent on this head, and that anchor is exa
- Cards §II's in-place replacement is not shown by this set. The capture's own log records the widget column drawing NO slot for 12 consecutive samples twice — at 03:06:14 and again at 03:07:50, 16 s after the gate was minted — and the driver re-opening both third-party pages each time. The pending frames were shot on the page opened at 03:07:52/03:08:03 after a fresh hosted sign-in, so the pictured pending card follows a page re-open rather than replacing a placeholder on its own. The set contain
- The first-render proof exists in light only: at the first-render instant (03:08:15.854) the dark context read reviewCards 0, cardState null and islandFramePresent false. 'previewDidNotLoad false' is trivially true there because no card existed to draw a panel.
- The reload persistence still does not hold by the stated mechanism: storedReviewGateParts 0 — no stored turn carries an artifact_review_gate part, so what survives the reload is projected from the run's own rows, not 'carried by the turn's durable content'. Unchanged from the earlier round.
- The record states two statuses for one run without naming the instants: measurements.json run.status reads 'pending_input' (in fact the creation-instant snapshot of 03:04:55) while the sequence log reads the same row 'completed' at 03:08:15. A readback quoting run id and status must say which instant it was read at.
- Re-measured and disclosed, standing: the run could not be released from inside the widget — the widget drew no schedule card and the release was pressed on the app's own run page at 03:05:12. That is the separate known defect on this host, present again in this run.
