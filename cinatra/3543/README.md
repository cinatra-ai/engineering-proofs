# Proof round 4 — the fourth picture round at 82b7edf9

A development boot of the branch at 82b7edf9d7d669760c036b2421b13825723cb2cc. One real agent run was
dispatched through the product's own Run-agent road, its context step and its review gate were settled
on the run's page, and the finished run's own readings were shot in both palettes.

## The run

- Agent: Blog Idea Generator Agent, the installed package version 0.2.0, kind agent, drawn active on the
  workspace extensions page before the dispatch.
- The run wrote five blog-idea artifacts. Its record step draws all five as rows with an Open link each.
- The review gate for the run was minted eight seconds after the run's own row already read completed;
  it was settled on the run's page, after which the record step stopped reading disabled.

## Counted cells

| frame | cell | palette |
| --- | --- | --- |
| cell1-record-step-light.png | the record step's own page | light |
| cell1-record-step-dark.png | the record step's own page | dark |
| cell2-rail-light.png | the rail of that finished run | light |
| cell2-rail-dark.png | the rail of that finished run | dark |
| cell3-artifact-page-light.png | the artifact's own page | light |
| cell3-artifact-page-dark.png | the artifact's own page | dark |

## Readings taken with the frames

- The record step reads with-output, its settled evidence reads outputs, one outputs list is drawn, and it
  holds five output links. The denying sentence appears 0 times and the loading sentence 0 times.
- The rail draws four entries — Schedule, Setup, Review and What this run made — and carries exactly one
  step mark per drawn entry. Each mark's box measures 208 px wide at left 464 and its own row's box
  measures 208 px wide at left 464: the same width and the same left edge, in both palettes.
- The settled review entry keeps its place third in the run's order with its settled mark reading true.
  The last entry's label is What this run made, and it reads selected with aria-current step after the press.
- The artifact page opened by pressing one output link is the artifact's own page under /artifacts carrying
  the id that link named, with the breadcrumb Artifacts and the title the row drew.

## Recorded, not counted

- parked-run-light.png, parked-run-dark.png — the run parked at its review gate before the decision.
- parked-run-review-step-light.png, parked-run-review-step-dark.png, parked-run-rail-light.png,
  parked-run-rail-dark.png — an earlier shutter set of the same parked page, taken before the gate was
  settled and named for what it actually shows; not shot as any counted cell.
- probe-dev-indicator.png — the throwaway probe frame whose bottom corners were looked at after the
  framework's development indicator was switched off through its own route.
- The wrench in the topbar is the development-only road artefact of a development boot and is never
  counted against any cell.
- The output link's own visible label reads Open; the row beside it draws the artifact's title.

Every location reading is a path alone. measurements.json carries each frame's digest, size, pixels and
luminance; dom-readings.jsonl carries one reading per counted shutter.
