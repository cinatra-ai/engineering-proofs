# Widget frame sign-in on a boot whose public address differs from its bind address — proof round 2 (#3330, pull request 3434)

One counted frame (1440 by 900 css pixels at device scale 2), shot on a development boot of the pull request's head through the product's own screens: the assistant widget open on a connected WordPress page, its frame served under the instance's saved public address, signed in through the product's own sign-in page, in its authenticated active state (the conversation column with its composer, no Sign in button). The framework's own development indicator was switched off through its own preference before the shutter and the editor's onboarding dialog dismissed; nothing in the picture is edited.

| file | cell | theme | what it shows |
|---|---|---|---|
| cell1-frame-authenticated-active-indicator-off-light.png | CELL1 | light | the widget's frame in its authenticated active state on the connected site |

The negative control of the same round (a foreign origin with forged matching headers refused) is a reading in the pull request's record, not a picture. Three earlier shutters of this cell are kept beside the round's readings on the coordinator side under attempt names and count for nothing.
