# Widget frame sign-in (#3330, pull request 3434) — the other frames the records name

Every frame the two proof-round records on the pull request name, so that none is a bare file name. Round 1 (16:38 → 17:41) stopped short of the signed-in state on a fixture gap; round 2 (17:44 → 19:08) reached it. The counted frame of round 2 is published beside these under the same folder.

| file | round | what it shows |
|---|---|---|
| cell1-frame-mounted-origin-admitted-signin-prompt-light.png | 1 | the widget open on the connected site's editor page, its frame mounted under the saved public address and admitted by the origin check, stopped at its sign-in prompt because the connector held no site record yet |
| precondition-connect-authorize-approval-masked.png | 1 | the instance's own approval screen for connecting the site (account row painted over) |
| precondition-connector-setup-connect-site-gated-masked.png | 1 | the connector's setup card offering only the connection-service configuration, the gap round 2 closed (account row painted over) |
| cell1-attempt-frame-waiting-for-signin-window-light.png | 2, attempt | the frame after the sign-in window closed, before the transition: "Waiting for the sign-in window" — counted for nothing |
| cell1-attempt-frame-authenticated-active-indicator-visible-light.png | 2, attempt | the signed-in state with the framework's own compiling pill over the composer — counted for nothing |
| cell1-attempt-frame-authenticated-active-indicator-button-light.png | 2, attempt | the signed-in state with the framework's own tools button over the composer's edge — counted for nothing |
