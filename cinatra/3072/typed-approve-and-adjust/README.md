# cinatra pull request 3072 — acceptance item 5 of #2853: a typed approve and a typed schedule adjust, each showing the card settling

Real runs through the app's own dispatch and the real provider; full browser window at 1440x900, device scale 2; light and dark through the app's own theme control; the person drawn in the sidebar footer on every frame.

| picture | luminance | requires | shows | verdict |
|---|---|---|---|---|
| `chat-review-typed-approve-before-light.png` | 236.63 | Drawing §I "The composer knows which card it answers" and plan §2.1 row 1: one open review card filling the assistant turn at the column's full width; a row above the card's buttons carrying the button "Replying to this review" shown pressed and the status line "Your next chat message becomes a comment on this review. Press again to chat normally.", its trailing line in muted; under it the subordinate note field and  | Full uncropped window 2880x1800, mean luminance 236.74 recomputed (236.63 recorded). Sidebar footer draws Jonas Beck / jonas.beck@example.com. One review card: header "Review requested" with the "Awaiting your decision" pill, target island reading "@cinatra-ai/blog-post-artifact:post · revision 8408b5dd-142… · pinned · Ownership: organization · Visibility: organization · text/markdown · updated 2026-08-29T06:03:40.18 | PASS on the state the frame is for — one card, bound with no press, the button, the sentence and the muted treatment exactly as drawn. Two defects in the picture: the card's decision floor is not shown at all (clipped behind the composer), so this frame does not evidence the drawn floor; and a launc |
| `chat-review-typed-approve-before-dark.png` | 18.61 | The same state as its light twin, in the app's own dark theme (the class the app sets, not colour-scheme emulation), with a dark-band luminance. | Identical layout and identical copy in dark; the top bar's theme control draws the moon; mean luminance 18.89 recomputed (18.61 recorded), far below the 128/255 refusal line; region 30.76 over the bound card. The binding row and status sentence are word-for-word the drawn ones. The same two "N" badges are present, one over the reviewed text inside the framed window. The decision floor is again behind the composer — o | PASS on the bound state and on the theme provenance. Same two defects as the light twin: the floor is not readable in the picture, and a launcher badge overlays the reviewed content. |
| `chat-review-typed-approve-settled-light.png` | 237.95 | Task frame 2 and plan §4.2's own words for the target settled state: "the settled card names the outcome and the decider itself, and the Refresh button disappears with the ambiguity that required it" — the card settled as approved, the transcript carrying the person's words and the assistant's confirmation, no new card, nothing stacked above. | The settled panel: a double-check mark over "Approved by Jonas Beck" and "The gate is resolved and the run has been released to continue." No Refresh, no decision floor, no binding row (the same-instant DOM gives buttons = ["Expand"] and the anchors review-gate-settled with no review-decision-bar). Under it the person's right-aligned filled bubble "approve it" and the assistant's left-aligned unbubbled "Approved." ca | PASS — this is the plan's target settled reading (outcome plus decider, Refresh gone), one card, and both turns are in the transcript. Recorded, not scored: the card's header is out of frame. |
| `chat-review-typed-approve-settled-dark.png` | 17.37 | The same settled reading in the app's own dark theme, in the dark luminance band. | Identical settled panel and identical transcript in dark; mean luminance 17.65 recomputed (17.37 recorded); region 28.36 over the settled card; moon theme control in the bar. cardCount = 1. developmentPill none. | PASS — same as the light twin, including the absent Refresh and the single card. |
| `chat-review-typed-approve-after-send-live-light.png` | 237.46 | Plan §4.1: "After the decision the card says in place what it did" — the line "Approved. The gate is resolved and the run has been released to continue." — and a resolved gate must not keep offering a terminal decision. | The transcript already carries "approve it" and "Approved.", and the gate was resolved in the database at 06:06:16.853Z. Yet the card above is drawn exactly as pending: "Replying to this review" still pressed with its bound status sentence, the DECISION RATIONALE field with "Add a note for the run and the audit trail…", and a fully live floor — Comment quiet at the left, Reject in the destructive treatment, Approve i | DEFECT. A decided card does not settle in the live page session: it keeps drawing pending and keeps a terminal Approve and Reject pressable over a gate that is already resolved, and the "says in place what it did" step of §4.1 is never drawn at all. Only re-opening the thread produces the settled re |
| `chat-review-typed-approve-after-send-live-dark.png` | 17.66 | The same reading in the app's own dark theme. | The same stale pending card with the live Comment / Reject / Approve floor over a resolved gate, in dark; mean luminance 17.95 recomputed (17.66 recorded). The Approve button inverts to a near-white fill with dark text (the dark form of the primary treatment; the drawing has no dark rendering to compare). developmentPill none. | DEFECT — the same one the light twin records: a resolved gate still drawn pending with its terminal controls live. |
| `chat-schedule-typed-adjust-before-light.png` | 238.06 | Plan §7.2 and drawing §VI: the body is the standard scheduling step, unchanged — "When should this run?" over three option rows, the chosen row taking the indigo edge and tint and owning its fields (Repeat every N week(s); On Sun–Sat; At HH:MM; Timezone); under them "Estimated run duration" with a range; no raw cron field anywhere; "The floor is Confirm". | The person's ask, then "Scheduling card shown. It proposes Blog Draft Writer Agent every weekday at 09:00 Europe/Berlin time.", then the card: "When should this run?", Run right after setup / Schedule for later / Recurring, with Recurring chosen and carrying the indigo edge and tinted ground, Repeat every 1 week(s), Mon–Fri filled indigo with Sun and Sat off, At 09 : 00, Timezone Europe/Berlin. No cron field anywhere | PASS against the plan's own §7.2 words — the rows, the chosen-row treatment, the absent cron and the Confirm-only floor are as specified. One defect: "Estimated run duration" draws "Unavailable." where the drawing draws a range, so the drawn reading is missing. One divergence to fix in the drawing,  |
| `chat-schedule-typed-adjust-before-dark.png` | 20.13 | The same pre-adjust card in the app's own dark theme. | The same three option rows and the same values (Recurring, 1 week(s), Mon–Fri, 09 : 00, Europe/Berlin, "Unavailable.", Confirm clipped by the composer) in dark; mean luminance 20.39 recomputed (20.13 recorded); region 35.17. The chosen Recurring row is marked by a light border only — the drawn indigo edge and tint invert to a near-white edge over the same ground, with no tint; the selected day chips invert to near-wh | PASS on the state and the values. Same "Unavailable." defect as the light twin. The dark treatment of the chosen row (edge without tint) is recorded, not scored — the drawing has no dark rendering of this card. |
| `chat-schedule-typed-adjust-redrawn-light.png` | 238.56 | Task frame 4 and plan §2.2 ("The prompt window acts on the active card … For the active card, the signed-in person can state in words any action that card already offers") with §7.2 ("the same card, with the same option rows … No second card is drawn"): the SAME card's rows showing the adjusted schedule, with the person's words in the transcript. | The person's "make it 8 in the morning on weekdays" at the top, then "Updated scheduling card shown. It proposes Blog Draft Writer Agent every weekday at 08:00 Europe/Berlin time.", then a card reading Recurring / Repeat every 1 week(s) / Mon–Fri (Sun, Sat off) / At 08 : 00 / Timezone Europe/Berlin / Estimated run duration Unavailable. / Confirm — the whole floor visible here, Confirm alone, right-aligned. The rows a | DEFECT. The typed adjust does not act on the active card: it mints a SECOND proposal below and leaves the stale 09:00 card live with its own Confirm, so the person can still confirm the schedule they asked to change — against §2.2's "acts on the active card" and §7.2's "No second card is drawn". Sec |
| `chat-schedule-typed-adjust-redrawn-dark.png` | 19.64 | The same post-adjust reading in the app's own dark theme. | The same new 08:00 card with the full Confirm-only floor, the person's words above it, in dark; mean luminance 19.90 recomputed (19.64 recorded); region 26.63. The DOM again records two trigger_schedule_proposal:pending cards, the 09:00 one out of frame above. developmentPill none. | DEFECT — the same one: a second card instead of the bound card re-drawn, the stale 09:00 card left live above with its own Confirm, and the stacking not visible in the picture. |
| `chat-two-cards-typed-decision-refused-light.png` | 237.10 | Plan §2.1 rule 4 and the drawing's "SEVERAL REVIEWS OPEN, NONE CHOSEN" panel with its "Borrowed, not invented" callout: each card's row reads "Reply from the chat box" over "More than one review is waiting. Choose the one you want to reply to — chat messages are not routed until you do.", that trailing line in mustard-ink; the message goes nowhere; both cards unchanged; the assistant answers in the conversation, nami | One of the two cards in view, its binding row carrying exactly the drawn button and the drawn sentence, its decision floor untouched (Comment / Reject / Approve). Then the person's "approve it" and the assistant's "2 cards are waiting for you, so nothing was done to any of them. Choose the one you mean — press its “Reply from the chat box” control — and say it again." The DOM records two artifact_review_gate:pending  | PASS on the substance — nothing was routed, both gates untouched, and the refusal is identifier-free and names the way out. Three defects: (1) the trailing line is drawn in ink RGB(21,33,58), not the mustard-ink the drawing requires for exactly this state (the bound reading measures the correct mute |
| `chat-two-cards-typed-decision-refused-dark.png` | 16.18 | The same refusal reading in the app's own dark theme, and — plan §4.1 — a card that "always shows you something; it is never blank", with the drawing's LOADING state drawing placeholder bars in the same box while the target window loads. | The refusal itself is identical to the light twin: "Reply from the chat box" over the several-reviews sentence, the floor Comment / Reject / Approve untouched, the assistant's refusal in full, two pending gates in the DOM. Mean luminance 16.49 recomputed (16.18 recorded). But the card's framed target window is completely blank: I measured the 1430x430 region as a uniform RGB(13,24,42) with stddev 0.00 — no artifact c | PASS on the refusal. DEFECT on the card: the loading skeleton draws literally nothing — a flat empty box where the drawing's LOADING state draws grey placeholder bars — so the reader is shown a blank panel with no indication that anything is loading. |
| `setup-secrets-env-managed-light.png` | 238.44 | The task's secrets readback: /setup/secrets open, asking nothing, no secret value on screen — one light frame. | The Setup masthead, the stepper ACCOUNT · KEY · NAME · SECRETS · MODEL all ticked, "Secrets" over "Cinatra hands its secrets to Nango, which stores and manages external API credentials and OAuth connections. Point this instance at the Nango it should use." The Secret key field is EMPTY under "Leave blank to keep the current saved key." The Server URL field shows only the greyed placeholder https://api.nango.dev under | PASS as the readback — the step asks nothing and no value is shown, which is what the fill proof (grep -c '^NANGO_SECRET_KEY=.\+' → 1) needed to be paired with. One correction to the task's own phrase, stated as measured: only the Server URL is declared environment-set; the Secret key is stored and  |

## Defects, as measured

- The typed schedule adjust does not act on the active card. `make it 8 in the morning on weekdays` minted no grant and produced no lifecycle_bound_card_decide row; the assistant re-called the producer and drew a SECOND proposal at 08:00, leaving the stale 09:00 card live above it with its own Confirm — so the schedule the person asked to change can still be confirmed. Against plan §2.2 ("the prompt window acts on the active card") and §7.2 ("No second card is drawn"). Frames: chat-schedule-typed-adjust-redrawn-light/dark.png.
- A decided review card does not settle in the live page session. The gate resolved at 06:06:16.853Z and the card kept drawing pending with a live terminal Approve and Reject over a resolved gate; §4.1's in-place "Approved. The gate is resolved and the run has been released to continue." line is never drawn. Only re-opening the thread settles it. Frames: chat-review-typed-approve-after-send-live-light/dark.png.
- The several-reviews status line is the wrong colour. "More than one review is waiting…" measures RGB(21,33,58) — body ink — where the drawing's "Borrowed, not invented" callout says the trailing line "turns mustard-ink only while more than one review is waiting to be chosen". The muted token is correct on the bound frames (RGB(90,100,119)), so the state-specific colour is simply not applied. Frames: chat-two-cards-typed-decision-refused-light/dark.png.
- The several-cards refusal drops §2.1's escape sentence. The transcript reads "2 cards are waiting for you, so nothing was done to any of them. Choose the one you mean — press its “Reply from the chat box” control — and say it again." — §2.1's closing "To keep chatting normally, press that control twice on any one of them." is gone (ambiguousComposerRefusal no longer exists on the branch; severalCardsWaitingRefusal replaces it). The copy change is not among the PR's named deviations.
- The review target's loading skeleton draws nothing. In chat-two-cards-typed-decision-refused-dark.png the framed window is a uniform RGB(13,24,42) box, 1430x430, stddev 0.00 — no content, no placeholder bars, no "The preview did not load" line — while the DOM carries review-target-island-skeleton. §4.1 says the card "always shows you something; it is never blank" and the drawing's LOADING state draws placeholder bars.
- The schedule card's "Estimated run duration" reads "Unavailable." on all four schedule frames, where the drawing draws a range ("About 45s – 3.4 hr.") and §7.2 says "Estimated run duration with a range". The drawn reading is missing.
- A dark round badge lettered "N" is drawn over the reviewed content inside the framed target window on every review frame (a second, identical one is pinned at the browser window's bottom-right corner on all thirteen). The drawing puts no control inside the target window, and this one sits on top of the artifact under review.
- Framing shortfalls that push three required readings out of the pictures and into the DOM notes: the review card's decision floor is behind the fixed composer in both "before" frames (only the clipped DECISION RATIONALE label shows); the stale 09:00 card is above the top edge in both "redrawn" frames, so the stacking the defect turns on is not visible; and only one of the two cards is in frame in both refusal frames, so "both cards unchanged" is not evidenced by the picture.

**Overall:** All thirteen frames are real, full-window, correctly themed (dark 16.49–20.39, light 236.7–238.7 on my own recomputation, every sha256 matching) and the two central claims of acceptance item 5 hold in the pixels — the typed approve settles the card as \"Approved by Jonas Beck\" with one card and both turns in the transcript, and a typed decision with two cards open routes nothing while both gates stay pending — but the typed schedule adjust does NOT act on the active card (it mints a second proposal and leaves the stale 09:00 card live with its own Confirm), a decided card keeps drawing pending with its terminal Approve and Reject live for the rest of the session, and five further defects the pictures record (ink instead of mustard-ink on the several-reviews line, the dropped \"press that control twice\" sentence, an empty loading skeleton, \"Unavailable.\" where a duration range is drawn, and a launcher badge over the reviewed content), so acceptance item 5's \"each showing the card settling\" is met for the approve and NOT met for the schedule adjust.
